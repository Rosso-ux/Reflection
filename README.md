# Reflection Lista

Projeto Java utilizando Reflection para criar uma ArrayList dinamicamente.

O programa instancia a lista, adiciona elementos usando o método `add()` via Reflection e exibe os valores no console.
